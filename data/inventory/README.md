# data/inventory/

| File / Folder | Purpose |
|---|---|
| `main_sections.json` | Discovered top-level sections of the main UET site (from the earlier/alternate `section_discovery.py` path, separate from the admission-specific `_sections.json` in `admission/`). |
| `admission/` | The full working directory for the admission-specific discovery pipeline — 85 files (crawled page data, PDF pipeline artifacts, OCR output, audits, and the final knowledge base). See `admission/README.md`. |
